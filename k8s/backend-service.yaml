apiVersion: v1
kind: Service
metadata:
  name: physicalc-backend
spec:
  selector:
    app: physicalc-backend
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

